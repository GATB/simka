#CMake is required to compile sitka (http://www.cmake.org/cmake/resources/software.html)
#you can install simka by executing this file:  sh INSTALL
# Prepare directories:
mkdir  build
# Go in the 'build' directory
cd build
# Prepare the makefile
cmake -DMAJOR=1 -DMINOR=0 -DPATCH=0 ..
# Run the newly created makefile:
make
#run the toy example
./simka -in ../example/simka_input.txt -out output/ -out-tmp temp_output

 
